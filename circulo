class Circulo:
    def __init__(self, radio):
        self.radio = radio
        self.diametro = radio * 2
    def calcular_area(self):
        return 3 * (self.radio * self.radio)
    def calcular_circunferencia(self):
        return 2 * 3 * self.radio
